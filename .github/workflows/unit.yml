name: Build

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.10"
      - uses: pre-commit/action@2c7b3805fd2a0fd8c1884dcaebf91fc102a13ecd # v3.0.1

  #mypy:
  #  runs-on: ubuntu-latest
  #  steps:
  #    - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
  #    - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
  #      with:
  #        python-version: "3.10"
  #    - name: Install nox
  #      run: pip install nox
  #    - name: run mypy checks
  #      run: nox -s mypy

  build:
    needs: [lint]
    runs-on: ubuntu-latest
    env:
      # Inject EE creds when triggered by push or by a same-repo PR. Forks see ''.
      EARTHENGINE_TOKEN: ${{ (github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository) && secrets.EARTHENGINE_TOKEN || '' }}
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
        include:
          - os: macos-latest # macos test
            python-version: "3.11"
          - os: windows-latest # windows test
            python-version: "3.11"
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install nox
        run: python -m pip install nox
      - name: test with pytest
        run: nox -s test -- -vv
      - name: codecov
        if: github.event_name == 'push' && matrix.python-version == '3.10' && runner.os == 'Linux'
        uses: codecov/codecov-action@75cd11691c0faa626561e295848008c8a7dddffe # v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          verbose: true
      - name: assess dead fixtures
        if: matrix.python-version == '3.10'
        run: nox -s dead-fixtures

  test_gee:
    needs: [build]
    # Run on push and on same-repo PRs; skip on fork PRs (no secrets available).
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    env:
      EARTHENGINE_TOKEN: ${{ secrets.EARTHENGINE_TOKEN }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
      - name: Install nox
        run: python -m pip install nox
      - name: test GEE smoke tests
        run: nox -s test_gee -- -vv

  entry-point:
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.10"
      - name: Install nox
        run: python -m pip install nox
      - name: test the entrypoints
        run: nox -s bin

  docs:
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"
      - name: Install nox
        run: python -m pip install nox
      - name: build the documentation and check for warnings
        run: nox -s docs

  apps:
    needs: [build]
    # Templates use SepalMap which calls init_ee() in its constructor; skip on
    # fork PRs since they have no EE creds.
    if: github.event_name == 'push' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    env:
      EARTHENGINE_TOKEN: ${{ secrets.EARTHENGINE_TOKEN }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: python -m pip install .[test]
      - name: build the template panel application
        run: pytest --nbmake pysepal/templates/panel_app/ui.ipynb
      - name: build the template map application
        run: pytest --nbmake pysepal/templates/map_app/ui.ipynb
